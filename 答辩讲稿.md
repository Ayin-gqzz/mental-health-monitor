# 大学生心理健康状况动态监测与预警系统 — 答辩讲稿

---

## 人员 1：整体架构、前后端基础、部署

---

**【开场 · 项目介绍】**

各位老师好，我是负责本项目整体架构设计、前后端基础搭建和部署的同学。我们做的是**大学生心理健康状况动态监测与预警系统**。

先说一下我们要解决的问题：目前高校心理健康工作主要依赖问卷普查和辅导员人工观察，存在三个痛点——**数据采集不连续**、**风险发现滞后**、**干预缺乏数据支撑**。我们的系统通过让学生每周提交生活方式数据，结合机器学习模型自动预测抑郁风险，让辅导员能够提前发现、精准干预。

**【技术选型】**

整体技术栈如图所示。后端采用 **FastAPI + SQLAlchemy**，前端是 **React 19 + TypeScript + Vite**，数据库支持 **SQLite 和 MySQL 双库切换**，机器学习用 **scikit-learn**，部署用 **Docker Compose** 一键编排。

为什么选 FastAPI？因为它原生支持异步、自带 OpenAPI 文档生成、Pydantic 做数据校验非常方便。前端选 React 19 配合 Zustand 做状态管理，Recharts 做可视化图表，整体开发效率很高。

**【整体架构】**

看一下整体架构图。最上层是前端，通过 Axios 发 HTTP 请求，携带 JWT Token 做身份认证。中间是 FastAPI 后端，分为三个路由模块：`auth` 负责登录注册，`student` 负责学生端 8 个接口，`counselor` 负责辅导员端 17 个接口。下面是数据层，包括 SQLAlchemy ORM、预聚合表和 ML 模型服务。

请求流程是这样的：前端登录后拿到 JWT Token，之后每次请求都带在 Header 里。后端通过 `get_current_user` 依赖解析 Token，`require_role` 做角色鉴权，确保学生只能看自己的数据，辅导员只能看本院系的数据，管理员看全部。

**【前端基础架构】**

前端这边，我搭建了统一的基础架构。`api/client.ts` 封装了 Axios 实例，自动注入 Token，401 时自动跳转登录页。`stores/authStore.ts` 用 Zustand 管理全局认证状态，包括角色、院系、是否管理员等信息。路由层面用 `RoleGuard` 做角色守卫，学生访问 `/student/*`，辅导员访问 `/counselor/*`，互不干扰。

页面布局是经典的侧边栏 + 顶栏 + 内容区。侧边栏根据角色动态渲染菜单项，管理员和辅导员看到的菜单是不同的。

**【Docker 部署】**

部署方面，我写了三个 Docker 配置文件。`docker-compose.yml` 编排了三个服务：MySQL 8.0 提供数据库、后端容器启动时自动执行完整的数据初始化流水线——先训练模型、再导入 10 万学生数据、模拟 12 周行为记录、最后批量评估。前端容器用 Vite 开发服务器提供页面，通过代理把 `/api` 转发到后端。

也就是说，只需要一条命令 `docker compose up`，两分钟内就能启动完整系统，包括 10 万学生的真实数据和训练好的 ML 模型。

**【双数据库支持】**

一个值得一提的设计是双数据库支持。开发环境用 SQLite，零配置直接跑；生产环境切 MySQL，只需要改一个环境变量 `DATABASE_URL`。代码里通过 `is_sqlite()` 函数判断数据库类型，SQL 语句用 `week_label()`、`today_expr()` 等辅助函数做兼容，上层业务代码完全不需要关心底层是哪个数据库。

以上是我的部分。下面请我们组第二位同学介绍具体的业务功能开发。

---

## 人员 2：三大端全部业务功能开发

---

**【开场】**

谢谢。我负责的是三个角色端——学生、辅导员、管理员——的全部业务页面和接口开发。总共 **19 个页面、33 个 API 接口**。

**【学生端 · 首页仪表盘】**

学生登录后进入首页仪表盘。这里聚合展示了四块信息：个人基本信息、最新行为数据、最新 ML 评估结果、以及最近一次每周测评的得分。下方是一个 12 周的压力趋势折线图，让学生直观看到自己压力水平的变化。

**【学生端 · 每周测评】**

每周测算是学生端的核心功能。学生每周可以提交一次，从五个维度自评：情绪状态、睡眠质量、学业压力、社交满意度、生活满意度，每个维度 1 到 5 分。系统自动按权重计算总分——情绪 25%、睡眠 20%、学业 20%、社交 15%、生活满意度 20%。

如果学生填写了留言，系统会做两件事：第一，自动运行 NLP 情感分析，计算情感得分；第二，调用大语言模型生成 AI 回复。学生在测评记录页面可以看到自己的留言、AI 的紫色渐变回复框、以及辅导员的人工回复。

**【辅导员端 · 学生管理】**

辅导员登录后，首页是工作台，展示学生总数、高风险人数、平均压力、抑郁率四个核心指标，还有风险分布饼图和各院系抑郁率柱状图。

学生列表页面支持按姓名搜索、按性别和风险等级筛选，分页展示。点击进入学生详情页，可以看到该学生的行为趋势图、评估历史、以及辅导员备注功能——辅导员可以给每个学生的评估添加备注，方便后续跟踪。

**【辅导员端 · 每周测评管理】**

每周测评管理页面是辅导员查看学生留言的地方。我们做了一个很实用的功能：**按情感得分排序**，最消极的排在最前面，让辅导员优先关注情绪最差的学生。

点开详情后，右侧会展示完整的 NLP 分析面板——情感分析用红/灰/绿三色圆圈表示消极/中性/积极，下方是心理话题分类的进度条，显示该学生的留言主要涉及哪些心理话题，比如学业压力、人际关系、情感问题等。还有关键词标签，展示 TF-IDF 提取的关键词和权重。

辅导员可以直接在这里回复学生，回复后学生端就能看到。

**【辅导员端 · 统计分析】**

统计分析页面包含三个统计检验：T 检验比较高风险和低风险群体在五个指标上的差异，卡方检验分析性别与抑郁的关联，相关性分析用 Pearson 和 Spearman 两种方法计算生活方式与抑郁的相关性。所有计算都在后端用 scipy 完成，前端用表格和柱状图展示结果。

**【辅导员端 · 群体画像】**

群体画像页面是我觉得最有价值的功能。后端用 K-Means 聚类把学生分成 4 个群体，前端用 PCA 散点图展示聚类结果，用雷达图对比各群体特征，用堆叠柱状图展示各群体的风险分布。

我们做了 16 种群体画像命名，比如"高压失眠型""学业专注型""网络依赖型""健康阳光型"等，每种都有基于五个维度偏离度自动生成的特征描述和干预建议。辅导员不需要自己分析数据，系统直接告诉他：这个群体压力高、睡眠差，建议安排一对一辅导。

**【管理员端】**

管理员和辅导员共用同一套后端接口，但有两点不同：第一，管理员看全院数据，普通辅导员只能看本院系；第二，管理员侧边栏多了"注册管理员"入口，可以创建新的管理员账号。管理员不需要"定期上报"和"预警总览"功能，因为这些是辅导员日常工作的工具。

**【数据隔离】**

最后说一下数据隔离。后端通过 `_get_dept_filter` 函数控制：管理员返回 None（不过滤），普通辅导员返回其所属院系。所有列表查询都会自动加上院系过滤条件，确保辅导员看不到其他院系的数据。

以上是我的部分。下面请第三位同学介绍数据库设计。

---

## 人员 3：数据库设计、表结构、视图、触发器、SQL 性能优化

---

**【开场】**

我负责的是数据库层面的全部工作，包括表结构设计、视图和触发器、索引优化、以及预聚合表的性能方案。

**【表结构设计】**

整个数据库有 **6 张核心表**。

第一张 `student_info` 是学生信息表，`student_id` 做主键，有年龄的 CHECK 约束限制 18 到 30 岁，GPA 限制 0 到 4.0。在 `department` 和 `gender` 上建了索引，因为列表查询经常按这两个字段筛选。

第二张 `behavior_log` 是行为日志表，每天追加记录，这是数据量最大的表，10 万学生乘以 12 周大约 **130 万行**。`year_week` 字段是我们预计算的周标签，格式是 `2026-W21`，避免每次查询都做日期函数计算。这张表上建了 6 个索引，覆盖了学生查询、日期排序、周聚合等所有高频场景。

第三张 `mental_assessment` 是 ML 评估结果表，存储每次模型预测的抑郁概率、风险等级和干预建议文本。`risk_level` 有三个值：high、medium、low。

第四张 `counselor_user` 是辅导员账号表，通过运行时迁移自动加了 `department` 和 `is_admin` 字段。

第五张 `risk_notification` 是预警通知表，由触发器自动写入。

第六张 `weekly_assessment` 是每周测评表，存储学生的五维评分和留言。

**【视图设计】**

我建了两个视图。

`v_latest_assessment` 用子查询取每个学生最新的评估记录，这是一个很常见的"取每组最新一条"的模式，用 `MAX(assessment_date)` 做关联。

`v_student_behavior_summary` 把行为日志和学生信息做聚合，输出每个学生的平均压力、睡眠、社交等指标，加上院系信息，方便统计分析直接查。

**【触发器】**

触发器 `trg_high_risk_alert` 是自动预警的核心。它挂在 `mental_assessment` 表的 INSERT 之后，当新插入的评估记录 `risk_level` 等于 `high` 时，自动往 `risk_notification` 表插一条预警通知。这样辅导员登录后就能在预警列表里看到新的高风险学生，不需要人工轮询。

**【索引优化 · 16 个索引】**

索引是性能优化的重点。我总共建了 **16 个索引**。

举几个关键的：

`idx_behavior_id_student` 覆盖了 `behavior_log(id, student_id)`，这是为了让"取每个学生最新一条行为记录"的 `MAX(id) GROUP BY student_id` 子查询走索引，避免全表扫描。

`idx_trends_cover` 是趋势查询的覆盖索引，`(record_date, year_week, stress_level)`，让 `WHERE record_date >= ? GROUP BY year_week AVG(stress_level)` 整个查询都在索引上完成，不用回表。

`idx_sls_dept` 是 `student_latest_stats(department)` 索引，让统计检验和聚类分析按院系过滤时走索引，避免 JOIN `student_info` 表。

**【预聚合表 · 性能关键】**

130 万行的原始数据，如果每次统计都直接查，响应时间会很慢。我的方案是**预聚合**——启动时或数据变化时，把高频查询的结果预先算好存到三张表里：

`weekly_behavior_stats` 按周、按院系聚合行为均值，大约 140 行，趋势查询直接读这张表，**毫秒级返回**。

`weekly_depression_stats` 按周、按院系统计抑郁人数。

`student_latest_stats` 每个学生一行，包含最新行为和最新评估的全部字段，**10 万行**。统计检验的 T 检验、卡方检验、相关性分析，以及群体画像的 K-Means 聚类，全都读这张表，不再扫 130 万行原始数据。

预聚合表在首次启动时自动刷新，通过 `refresh_weekly_stats.py` 脚本实现。我还加了请求时兜底检查——如果端点发现表为空但原始数据存在，会自动触发刷新。

**【慢查询日志】**

最后，我在数据库层加了慢查询日志，超过 100 毫秒的 SQL 会自动记录到日志里，方便定位性能瓶颈。

以上是我的部分。下面请第四位同学介绍机器学习模块。

---

## 人员 4：专职机器学习模块

---

**【开场】**

我负责的是机器学习模块，从数据预处理、特征工程、模型训练、到后端预测服务的完整链路。

**【数据与目标】**

我们的数据集包含 10 万条学生记录，每条有 8 个原始特征：年龄、性别、院系、GPA、睡眠时长、学习时长、社交媒体使用时长、运动时长、压力水平。预测目标是二分类：是否抑郁（`Depression` 列）。

**【特征工程 · 16 个特征】**

原始 8 个特征不够，我做了特征工程扩展到 16 个。

首先，性别做 Label Encoding（Male=0, Female=1），院系做 One-Hot 编码生成 5 个虚拟变量（Arts、Business、Engineering、Medical、Science）。

然后我构造了 3 个衍生特征：

`Stress_Change_Rate`——压力变化率，衡量当前压力偏离中位数的程度，公式是 `(stress - median) / median`。这个特征捕捉的是"相对异常"，比如中位数是 5 的情况下，压力 7 和压力 9 的变化率是不同的。

`Sleep_Quality_Index`——睡眠质量指数，`sleep_duration × (1 - stress/10)`。压力越高，同样时长的睡眠质量越差。这个交互特征比单独用睡眠时长更有区分力。

`Lifestyle_Score`——生活方式综合分，加权计算：睡眠 30%、运动 20%、压力反向 20%、学习 15%、社交媒体反向 15%。这是一个综合健康指标。

所有 10 个数值特征做 StandardScaler 标准化，**只在训练集上 fit**，测试集用训练集的参数 transform，严格防止数据泄漏。

**【模型训练】**

算法选的是 **随机森林分类器**，用 GridSearchCV 做超参数调优。

为什么选随机森林？第一，它天然支持特征重要性排序，我们能知道哪些因素对抑郁预测影响最大；第二，它对不平衡数据比较鲁棒，配合 `class_weight="balanced_subsample"` 自动调整类别权重；第三，它不容易过拟合。

超参数搜索空间：树的数量 100、200、300，最大深度 10、15、20、不限，最小分裂样本数 2、5、10，最小叶节点样本数 1、2、4。总共 3×4×3×3 = 108 种组合，用 3 折分层交叉验证，评分指标选 ROC-AUC。

**【最优阈值 · Youden's J】**

分类默认阈值是 0.5，但我们的场景更关注"不漏检"，所以用 **Youden's J 统计量** 自动找最优阈值。方法是遍历 ROC 曲线上所有点，找到 `TPR - FPR` 最大的那个点对应的阈值。这样在保持较低误报率的同时，最大化了召回率。

**【模型评估】**

评估结果：ROC 曲线的 AUC 值达到了比较高的水平，PR 曲线的平均精确率也很理想。学习曲线显示训练集和验证集的 F1 分数随着样本量增加逐渐收敛，没有严重的过拟合。

这三个图表的数据在训练时就保存到了 `evaluation_curves.pkl`，前端直接读取展示，不需要实时计算。

**【模型服务 · MLService】**

模型训练完后保存为 6 个 pkl 文件：模型本体、标准化器、特征列名、数值列名、特征元数据、评估曲线数据。

后端的 `MLService` 用**单例模式**实现，进程启动时只加载一次模型到内存。`predict` 方法接收学生画像和行为数据，走完整的特征工程流程——编码、构造衍生特征、标准化——然后调用 `model.predict_proba` 得到概率，再用最优阈值判断是否抑郁。

风险等级的逻辑是三层判断：如果模型预测抑郁 **或者** 压力 ≥ 8，判为高风险；压力 ≥ 6，判为中风险；其余为低风险。这样即使模型概率没过阈值，极高的压力水平也会触发高风险预警。

**【批量评估】**

`assess_all.py` 脚本对全校 10 万学生做批量评估。为了性能，我用了一个 SQL 窗口函数 `ROW_NUMBER() OVER (PARTITION BY student_id ORDER BY record_date DESC)` 直接在数据库层面取每个学生的最新记录，避免 N+1 查询。评估结果批量插入，每 5000 条一批。

以上是我的部分。下面请第五位同学介绍 NLP 模块和项目整合。

---

## 人员 5：NLP 情感分析 + 前端复杂可视化 + 项目整合

---

**【开场】**

我负责的是 NLP 情感分析引擎、前端复杂可视化页面，以及项目整合、文档和答辩收尾工作。

**【NLP 引擎 · PsychAssessmentEngine】**

我开发了一个专门针对中文心理咨询场景的 NLP 引擎 `PsychAssessmentEngine`，核心功能有四个：情感分析、关键词提取、词频统计、心理话题分类。

**【情感分析】**

情感分析采用**双信号融合**方案。主信号是词典法：我构建了心理学专用的情感词典，正面词 74 个，比如"开心""释然""感恩"；负面词 100 多个，比如"焦虑""崩溃""绝望""失眠"。还加了否定词表，像"不开心"会翻转情感极性。

词典的权重参考了 PsyQA 心理问答语料库的词频，高频词给更高权重。计算时先做 Jieba 中文分词，去停用词（130 多个），然后遍历情感词典匹配，加权求和后归一化到 0 到 1 之间。低于 0.45 判为消极，高于 0.75 判为积极，中间是中性。

**【心理话题分类】**

我定义了 **8 个心理话题类别**，每个类别有 10 到 22 个关键词。比如"学业压力"类别下有"挂科""考研""绩点""毕业论文"等 22 个关键词；"人际关系"下有"室友""孤独""社交恐惧""被排挤"等 20 个。

分类方法是：先用 TF-IDF 提取留言中的关键词，然后把每个关键词和 8 个类别做匹配，匹配到的就把该关键词的 TF-IDF 权重累加到对应类别上。最后按得分排序，展示得分最高的几个话题。这样辅导员一眼就能看出这个学生主要困扰是什么。

**【数据结构选型】**

代码里用了多种数据结构，并且有明确的设计考量：

- **Set** 存停用词和话题关键词，O(1) 查找
- **Dict** 存词频映射和话题得分，O(1) 更新
- **List** 存分词序列和关键词-权重对，保持顺序
- **Tuple** 存 TF-IDF 关键词-权重对，不可变，方便排序

**【前端可视化】**

前端复杂可视化部分，我主要做了三个页面。

**群体画像页面**：后端返回聚类数据后，前端用 Recharts 画了五种图表——PCA 散点图展示聚类分布，雷达图对比各群体特征，堆叠柱状图展示风险分布，特征对比表格展示具体数值，还有特征描述和干预建议卡片。散点图限制最多 2000 个点避免前端卡顿，雷达图把五个不同量纲的特征归一化到 0-100 方便对比。

**每周测评管理页面**：辅导员可以看到 NLP 分析面板——情感分析的三色圆圈、心理话题的进度条、关键词的标签云。情感排序功能让辅导员优先看到最消极的学生。

**模型评估页面**：ROC 曲线、PR 曲线、学习曲线三个图表，数据来自训练时预计算的 pkl 文件。

**【项目整合】**

整合方面，我做了几件事：

第一，**前后端联调**。33 个 API 接口全部打通，前端的 Axios 封装、JWT 拦截器、401 自动跳转、角色守卫路由，都是我和第一、第二位同学协同完成的。

第二，**数据初始化流水线**。`seed_db.py` 导入 10 万学生 → `simulate_behavior.py` 模拟 12 周行为 → `assess_all.py` 批量 ML 评估 → `refresh_weekly_stats.py` 刷新预聚合表。这条流水线在 Docker 启动时自动执行，也在本地开发时手动运行。

第三，**边界情况处理**。比如每周测评限制每人每周只能提交一次；辅导员只能看本院系学生数据；管理员和辅导员的侧边栏菜单不同；登录时如果角色选错会有明确的错误提示。

**【总结】**

最后总结一下我们项目的几个亮点：

1. **端到端的完整链路**：从数据采集 → ML 预测 → 预警触发 → 辅导员干预 → 学生反馈，形成闭环。
2. **10 万级数据的性能优化**：预聚合表、覆盖索引、MiniBatchKMeans、多级缓存，确保大数据量下依然秒级响应。
3. **NLP + ML 双引擎**：不仅有机器学习做抑郁预测，还有 NLP 引擎做文本情感分析和话题分类，多维度刻画学生心理健康状态。
4. **16 种群体画像**：基于特征偏离度自动生成精准的群体命名、特征描述和干预建议，辅导员无需数据分析背景就能理解。
5. **一键部署**：`docker compose up` 一条命令，两分钟内启动完整系统。

以上就是我们组的全部内容，谢谢各位老师。有没有什么问题？

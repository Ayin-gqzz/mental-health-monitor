# 大学生心理健康状况动态监测与预警系统 — 系统设计文档

---

## 目录

1. [系统架构设计](#一系统架构设计)
2. [数据库设计](#二数据库设计)
3. [API 接口设计](#三api-接口设计)
4. [前端架构设计](#四前端架构设计)
5. [机器学习流水线设计](#五机器学习流水线设计)
6. [NLP 情感分析设计](#六nlp-情感分析设计)
7. [AI 智能回复设计](#七ai-智能回复设计)
8. [安全与权限设计](#八安全与权限设计)
9. [性能优化设计](#九性能优化设计)
10. [部署架构设计](#十部署架构设计)
11. [设计亮点汇总](#十一设计亮点汇总)

---

## 一、系统架构设计

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    表现层（Presentation）                     │
│    React 19 + TypeScript + Vite + Zustand + Recharts        │
│    职责：UI 渲染、状态管理、路由、图表可视化                   │
└─────────────────────────────┬───────────────────────────────┘
                              │ HTTP / JSON
┌─────────────────────────────▼───────────────────────────────┐
│                    接口层（API Gateway）                      │
│    FastAPI + Pydantic v2 + JWT 认证 + 角色权限               │
│    职责：请求校验、认证授权、参数解析、响应序列化              │
├─────────────────────────────────────────────────────────────┤
│                    业务层（Service）                          │
│    MLService · SuggestionService · NLPService                │
│    职责：业务逻辑、ML 推荐、干预建议生成、情感分析             │
├─────────────────────────────────────────────────────────────┤
│                    数据层（Data Access）                      │
│    SQLAlchemy ORM + 原生 SQL + 预聚合表                      │
│    职责：数据持久化、查询优化、跨数据库兼容                    │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    基础设施层（Infrastructure）               │
│    SQLite / MySQL · Docker Compose · 文件系统                │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 模块依赖关系

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  auth.py │     │student.py│     │counselor │
│  认证模块 │     │ 学生模块  │     │ 辅导员模块 │
└────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │
     ▼                ▼                ▼
┌────────────────────────────────────────────┐
│           security.py (JWT + bcrypt)        │
│           require_role() 权限依赖           │
└────────────────────────────────────────────┘
     │                │                │
     ▼                ▼                ▼
┌──────────┐     ┌──────────┐     ┌──────────┐
│ml_service│     │suggestion│     │nlp_service│
│ ML 预测   │     │ 干预建议  │     │ 情感分析   │
└──────────┘     └──────────┘     └──────────┘
     │                │                │
     ▼                ▼                ▼
┌────────────────────────────────────────────┐
│        SQLAlchemy Models + SQL Views        │
│        student_info · behavior_log · ...    │
└────────────────────────────────────────────┘
```

### 1.3 ⭐ 设计亮点：生命周期管理

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动时：建表 → 迁移 → 建索引 → 建视图/触发器 → 回填数据 → 刷新预聚合表
    Base.metadata.create_all(bind=engine)
    with engine.begin() as conn:
        _auto_migrate(conn)        # 自动迁移：检测并添加新列
        _create_indexes(conn)      # 创建索引
        _create_views(conn)        # 创建视图
        _create_triggers(conn)     # 创建触发器
        _backfill_notifications(conn)  # 回填预警通知
        _refresh_pre_agg(conn)     # 刷新预聚合表
    yield
```

**亮点**：应用启动时自动完成所有数据库初始化，无需手动执行 SQL 脚本。使用 `_col_exists()` 检测列是否存在，实现幂等迁移。

---

## 二、数据库设计

### 2.1 ER 图

```
┌──────────────────┐       ┌──────────────────┐
│   student_info   │       │  counselor_user   │
├──────────────────┤       ├──────────────────┤
│ student_id (PK)  │       │ id (PK)          │
│ name             │       │ username          │
│ age              │       │ password_hash     │
│ gender           │       │ display_name      │
│ department       │       │ department        │
│ cgpa             │       │ is_admin          │
│ password_hash    │       └────────┬─────────┘
└────────┬─────────┘                │
         │                          │ counselor_id (FK)
         │                          │
    ┌────┴────┬─────────┬───────────┼───────────┐
    │         │         │           │           │
    ▼         ▼         ▼           ▼           ▼
┌────────┐┌────────┐┌────────┐┌──────────┐┌──────────┐
│behavior││mental  ││risk    ││weekly    ││counselor │
│_log    ││assess  ││notific ││assess    ││_report   │
├────────┤├────────┤├────────┤├──────────┤├──────────┤
│id (PK) ││id (PK) ││id (PK) ││id (PK)   ││id (PK)   │
│stu_id  ││stu_id  ││stu_id  ││stu_id    ││counselor │
│date    ││date    ││risk    ││mood      ││_id       │
│sleep   ││predict ││level   ││sleep     ││dept      │
│study   ││prob    ││message ││study     ││week      │
│social  ││risk    ││is_read ││social    ││status    │
│activity││level   ││        ││satisf    ││abnormal  │
│stress  ││interv  ││        ││overall   ││key_stu   │
│year_wk ││year_wk ││        ││message   ││          │
│        ││notes   ││        ││ai_reply  ││          │
│        ││        ││        ││reply     ││          │
│        ││        ││        ││sentiment ││          │
└────────┘└────────┘└────────┘└──────────┘└──────────┘
```

### 2.2 ⭐ 设计亮点：预聚合表

```
┌─────────────────────────────────────────────────────────────┐
│                    原始数据（120 万行）                        │
│  behavior_log: student_id, record_date, stress_level, ...   │
│  mental_assessment: student_id, depression_predicted, ...    │
└─────────────────────────┬───────────────────────────────────┘
                          │ 定时刷新（refresh_weekly_stats.py）
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    预聚合表（~180 行）                         │
│  weekly_behavior_stats: year_week, department, avg_stress    │
│  weekly_depression_stats: year_week, department, count       │
│  student_latest_stats: student_id, 最新行为+评估              │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
              周趋势查询：毫秒级响应
              聚类分析：直接读 10 万行，不扫 120 万行
```

**亮点**：
- 将百万行的聚合查询转化为预计算的统计表，查询时间从秒级降到毫秒级
- 按院系 + 周次双维度聚合，支持灵活的过滤和对比
- `student_latest_stats` 为聚类分析和统计检验提供单表数据源

### 2.3 ⭐ 设计亮点：触发器自动预警

```sql
CREATE TRIGGER trg_high_risk_alert
AFTER INSERT ON mental_assessment
WHEN NEW.risk_level = 'high'
BEGIN
    INSERT INTO risk_notification (student_id, risk_level, message, is_read, created_at)
    VALUES (NEW.student_id, 'high',
            'Student ' || NEW.student_id || ' assessed as HIGH risk on ' || NEW.assessment_date,
            0, NEW.created_at);
END;
```

**亮点**：数据库层面自动触发预警通知，无需应用代码干预。即使批量插入 10 万条评估记录，每条高风险记录都会自动生成通知。

### 2.4 ⭐ 设计亮点：视图简化查询

```sql
-- 最新评估视图：避免每次写 MAX(id) 子查询
CREATE VIEW v_latest_assessment AS
SELECT ma.*
FROM mental_assessment ma
INNER JOIN (
    SELECT student_id, MAX(assessment_date) AS max_date
    FROM mental_assessment
    GROUP BY student_id
) latest ON ma.student_id = latest.student_id
    AND ma.assessment_date = latest.max_date
```

**亮点**：将复杂的窗口函数查询封装为视图，业务代码直接 `SELECT * FROM v_latest_assessment` 即可获取最新评估。

### 2.5 索引设计策略

| 索引 | 目标查询 | 设计思路 |
|------|----------|----------|
| `(student_id, assessment_date DESC)` | 某学生的最新评估 | 覆盖索引，避免回表 |
| `(id, student_id)` | MAX(id) GROUP BY | 覆盖索引让聚合走索引 |
| `(risk_level, is_read, created_at DESC)` | 预警列表过滤+排序 | 复合索引覆盖 WHERE + ORDER BY |
| `(record_date, year_week, stress_level)` | 周趋势统计 | 覆盖索引让 AVG() 走索引 |
| `(year_week)` | 按周次查询 | 预聚合表主键 |

---

## 三、API 接口设计

### 3.1 RESTful 资源设计

```
/api/auth/
├── POST   /login              # 学生登录
├── POST   /login/counselor    # 辅导员登录
├── POST   /register           # 学生注册
├── POST   /register/counselor # 辅导员注册
└── PUT    /counselor/password # 修改密码

/api/student/
├── GET    /dashboard          # 首页数据（概况+趋势+评估）
├── GET    /behavior           # 行为记录列表
├── GET    /behavior/trend     # 12 周行为趋势
├── GET    /assessments        # 评估记录列表
├── GET    /weekly-assessments # 每周测评历史
└── POST   /weekly-assessments # 提交每周测评（含 AI 自动回复）

/api/counselor/
├── GET    /statistics/overview        # 总览统计
├── GET    /statistics/departments     # 各院系统计
├── GET    /statistics/trends          # 周趋势数据
├── GET    /statistics/stress-dist     # 压力分布
├── GET    /statistics/t-test          # T 检验
├── GET    /statistics/chi-square      # 卡方检验
├── GET    /statistics/correlation     # 相关性分析
├── GET    /statistics/complex-query   # SQL 优化演示
├── GET    /students                   # 学生列表
├── GET    /students/{id}              # 学生详情
├── POST   /students/{id}/assess       # 触发单人评估
├── POST   /assess-all                 # 批量评估
├── PUT    /students/{id}/notes        # 更新备注
├── GET    /alerts                     # 预警列表
├── PUT    /notifications/{id}/read    # 标记已读
├── PUT    /notifications/read-all     # 全部已读
├── GET    /weekly-assessments         # 测评列表
├── GET    /weekly-assessments/{id}    # 测评详情（含 AI 回复）
├── POST   /weekly-assessments/{id}/reply   # 辅导员回复
├── GET    /cluster-analysis           # 聚类分析
├── GET    /model/evaluation           # 模型评估
├── GET    /reports                    # 汇报列表
├── POST   /reports                    # 提交汇报
└── GET    /reports/latest             # 各院系最新汇报
```

### 3.2 ⭐ 设计亮点：统一分页响应

```python
class PaginatedResponse(BaseModel):
    items: list
    total: int
    page: int
    page_size: int
    total_pages: int
```

**亮点**：所有列表接口统一返回 `PaginatedResponse`，前端无需为每个接口写不同的解析逻辑。

### 3.3 ⭐ 设计亮点：权限依赖注入

```python
router = APIRouter(dependencies=[Depends(require_role("counselor"))])

def require_role(role: str):
    def checker(current_user: dict = Depends(get_current_user)) -> dict:
        if current_user["role"] != role:
            raise HTTPException(403, "Insufficient permissions")
        return current_user
    return checker
```

**亮点**：
- 路由级别统一注入权限检查，无需在每个端点重复校验代码
- 返回 `user` 字典包含 `db`（数据库会话）、`user_id`、`department`、`is_admin`，一处认证全局可用

### 3.4 ⭐ 设计亮点：院系数据隔离

```python
def _get_dept_filter(user: dict) -> str | None:
    """admin 返回 None（看全部），普通辅导员返回自己的院系"""
    if user.get("is_admin"):
        return None
    return user.get("department")

# 使用方式
dept = _get_dept_filter(user)  # None 或 "Engineering"
if dept:
    q = q.filter(StudentInfo.department == dept)
```

**亮点**：一个函数统一处理管理员和普通辅导员的数据权限，admin 看全院数据，普通辅导员只能看本院系。

---

## 四、前端架构设计

### 4.1 组件结构

```
src/
├── api/                    # API 层
│   ├── client.ts           # Axios 实例 + JWT 拦截器
│   ├── auth.ts             # 认证 API
│   ├── student.ts          # 学生 API
│   └── counselor.ts        # 辅导员 API（带 TTL 缓存）
├── components/
│   ├── layout/
│   │   ├── AppLayout.tsx   # 主布局（侧边栏 + 顶栏 + Outlet）
│   │   └── Sidebar.tsx     # 侧边导航（角色自适应）
│   └── guards/
│       ├── ProtectedRoute.tsx  # 认证守卫
│       └── RoleGuard.tsx       # 角色守卫
├── pages/
│   ├── student/            # 学生页面（6 个）
│   │   ├── StudentDashboard.tsx
│   │   ├── BehaviorHistory.tsx
│   │   ├── AssessmentHistory.tsx
│   │   ├── WeeklyAssessmentPage.tsx
│   │   ├── WeeklyAssessmentHistory.tsx
│   │   └── PsychChatPage.tsx      # 心理咨询聊天
│   └── counselor/          # 辅导员页面（12 个）
│       ├── CounselorDashboard.tsx
│       ├── StudentList.tsx
│       ├── StudentDetail.tsx
│       ├── AlertsPage.tsx
│       ├── StatisticsPage.tsx
│       ├── StatAnalysisPage.tsx
│       ├── ClusterAnalysisPage.tsx
│       ├── WeeklyReviewPage.tsx
│       ├── ReportPage.tsx
│       ├── ChangePasswordPage.tsx
│       └── RegisterCounselorPage.tsx
├── stores/
│   └── authStore.ts        # Zustand 认证状态（持久化）
└── App.tsx                 # 路由配置
```

### 4.2 ⭐ 设计亮点：路由守卫双层防护

```tsx
<Route element={<ProtectedRoute><AppLayout /></ProtectedRoute>}>
  <Route path="/counselor" element={
    <RoleGuard role="counselor"><CounselorDashboard /></RoleGuard>
  } />
  <Route path="/student" element={
    <RoleGuard role="student"><StudentDashboard /></RoleGuard>
  } />
</Route>
```

**亮点**：
- **第一层** `ProtectedRoute`：检查是否已登录（token 是否存在）
- **第二层** `RoleGuard`：检查角色是否匹配（counselor/student）
- 两层守卫独立运作，职责清晰

### 4.3 ⭐ 设计亮点：角色自适应侧边栏

```tsx
const adminLinks = [
  { group: "数据总览", items: [...] },
  { group: "系统管理", items: [...] },
];

const counselorBasicLinks = [
  { group: "学生管理", items: [...] },
  { group: "个人设置", items: [...] },
];

const studentLinks = [
  { path: "/student", label: "首页", icon: LayoutDashboard },
  { path: "/student/psych-chat", label: "心理咨询", icon: Heart },
  // ...
];

// 根据角色动态切换
{role === "counselor" ? (isAdmin ? adminLinks : counselorBasicLinks) : studentLinks}
```

**亮点**：管理员、普通辅导员、学生使用不同的侧边栏配置，通过角色动态切换，无需重复组件。

### 4.4 ⭐ 设计亮点：JWT 自动续期 + 401 重定向

```typescript
// 请求拦截器：自动添加 Token
client.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截器：401 自动跳转登录
client.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) {
      localStorage.clear();
      window.location.href = "/login";
    }
    return Promise.reject(err);
  }
);
```

**亮点**：全局拦截器统一处理认证，无需在每个 API 调用中手动处理 401 错误。

### 4.5 ⭐ 设计亮点：前端 TTL 缓存

```typescript
const _cache = new Map<string, { data: any; time: number }>();

function cached(key: string, fetcher: () => Promise<any>, ttl = 300000): Promise<any> {
  const hit = _cache.get(key);
  if (hit && Date.now() - hit.time < ttl) return Promise.resolve(hit.data);
  return fetcher().then((data) => {
    _cache.set(key, { data, time: Date.now() });
    return data;
  });
}
```

**亮点**：
- 只缓存第一页无筛选的请求，避免返回过期数据
- TTL 默认 5 分钟，预警数量缓存 30 秒
- 切换筛选条件时自动绕过缓存

### 4.6 ⭐ 设计亮点：双 Y 轴图表

```tsx
<LineChart data={trends}>
  <YAxis yAxisId="left" domain={[0, 10]} />
  <YAxis yAxisId="right" orientation="right" />
  <Line yAxisId="left" dataKey="avg_stress" stroke="#ef4444" name="平均压力" />
  <Line yAxisId="right" dataKey="depression_count" stroke="#8b5cf6" name="抑郁人数" />
</LineChart>
```

**亮点**：平均压力（0~10）和抑郁人数（0~26000）量级差异大，使用双 Y 轴让两条线都清晰可见。

---

## 五、机器学习流水线设计

### 5.1 训练流程

```
┌─────────────────────────────────────────────────────────────┐
│                    数据准备阶段                               │
│  1. 读取 CSV（10 万学生）                                     │
│  2. 合并行为数据（最新一周）                                   │
│  3. 特征工程（原始 10 + 派生 3 = 16 特征）                    │
│  4. 类别编码（Gender, Department → One-Hot）                  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据平衡阶段                               │
│  SMOTE 过采样：少数类（抑郁）生成合成样本                      │
│  处理前：抑郁 15% / 非抑郁 85%                                │
│  处理后：抑郁 50% / 非抑郁 50%                                │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    模型训练阶段                               │
│  Random Forest + GridSearchCV（5 折交叉验证）                 │
│  超参数搜索：                                                │
│    n_estimators: [100, 200, 300]                             │
│    max_depth: [10, 15, 20, None]                             │
│    min_samples_split: [2, 5, 10]                             │
│    min_samples_leaf: [1, 2, 4]                               │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    模型评估与保存                             │
│  输出：model.pkl, scaler.pkl, feature_cols.pkl, ...          │
│  评估曲线：ROC, PR, Learning Curve → evaluation_curves.pkl   │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 ⭐ 设计亮点：派生特征工程

```python
# 睡眠压力比：睡眠好 + 压力低 → 比值高 → 风险低
df["sleep_stress_ratio"] = df["Sleep_Duration"] / (df["Stress_Level"] + 1)

# 社交运动比：社交多 + 运动少 → 比值高 → 风险高
df["social_activity_ratio"] = df["Social_Media_Hours"] / (df["Physical_Activity"] / 60 + 0.1)

# 学习效率：绩点高 + 学习时间短 → 效率高 → 风险低
df["study_efficiency"] = df["CGPA"] / (df["Study_Hours"] + 0.1)
```

**亮点**：通过领域知识构造派生特征，捕捉变量间的交互关系，比单纯使用原始特征提升模型表现。

### 5.3 ⭐ 设计亮点：单例懒加载模型

```python
class MLService:
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance._load_model()
        return cls._instance

    def _load_model(self):
        self.model = joblib.load("ml/model.pkl")
        self.scaler = joblib.load("ml/scaler.pkl")
        # ...
```

**亮点**：
- 全局单例：模型只加载一次，节省内存和启动时间
- 懒加载：首次调用 `predict()` 时才加载，避免启动时阻塞
- 10 万学生批量评估时复用同一实例

### 5.4 ⭐ 设计亮点：双层风险判定

```python
# 第一层：ML 模型二分类预测
predicted = model.predict(scaled_features)[0]  # True/False
proba = model.predict_proba(scaled_features)[0][1]  # 0.0~1.0

# 第二层：规则引擎叠加
if predicted or behavior["stress_level"] >= 8:
    risk = "high"
elif behavior["stress_level"] >= 6:
    risk = "medium"
else:
    risk = "low"
```

**亮点**：
- ML 模型负责模式识别（从数据中学习抑郁特征）
- 规则引擎负责硬性指标（压力 ≥ 8 直接高风险）
- 两层结合，既利用数据驱动又保留业务规则

### 5.5 ⭐ 设计亮点：14 种干预建议模板

| 类别 | 模板 | 触发条件 |
|------|------|----------|
| 压力 | 高压预警 / 中等压力 | stress ≥ 8 / ≥ 6 |
| 睡眠 | 严重不足 / 不足 | sleep < 4h / < 6h |
| 运动 | 缺乏 / 可提升 | activity < 60 / < 120 min |
| 社交媒体 | 过度 / 提醒 | social > 4h / > 2h |
| 学习 | 过劳 / 不足 | study ≥ 10h / ≤ 2h |
| 抑郁风险 | 专项提示（含 24h 危机热线） | 概率 ≥ 30% 或模型预测 |
| 综合 | 良好 / 轻度关注 | 无明显问题时 |

**亮点**：根据多个维度组合生成个性化建议，不是简单的固定文本，而是有针对性地回应学生的具体问题。

---

## 六、NLP 情感分析设计

### 6.1 分析流程

```
学生留言文本
    ↓
┌─────────────────────────────────────────┐
│  1. jieba 分词 + 停用词过滤              │
│  2. 情感词典匹配 → 情感得分（-1 ~ +1）   │
│  3. TF-IDF 关键词提取（Top 10）          │
│  4. 心理主题分类（8 个维度）              │
└─────────────────────────────────────────┘
    ↓
输出：情感分 + 关键词 + 心理主题分布
```

### 6.2 心理主题分类（8 个维度）

| 维度 | 关键词示例 | 说明 |
|------|-----------|------|
| 情绪困扰 | 焦虑、抑郁、难过、痛苦 | 负面情绪状态 |
| 人际关系 | 宿舍、室友、朋友、吵架 | 社交关系问题 |
| 学业压力 | 考试、挂科、作业、GPA | 学习相关压力 |
| 睡眠问题 | 失眠、熬夜、睡不着 | 睡眠障碍 |
| 家庭关系 | 父母、家里、家庭 | 家庭相关问题 |
| 自我认同 | 自卑、迷茫、价值 | 自我认知问题 |
| 经济压力 | 兼职、钱、经济 | 经济相关压力 |
| 未来焦虑 | 就业、前途、考研 | 对未来的担忧 |

### 6.3 ⭐ 设计亮点：关键词权重评分

```python
# 情感词典带权重
sentiment_dict = {
    "开心": 0.9, "满意": 0.7,     # 正面
    "焦虑": -0.8, "抑郁": -0.9,   # 负面
    "一般": 0.0, "还好": 0.2,     # 中性
}

# 程度副词加权
degree_dict = {
    "非常": 2.0, "很": 1.5,       # 加强
    "有点": 0.5, "稍微": 0.3,     # 减弱
}

# 否定词反转
negation_dict = {"不": -1, "没": -1, "别": -1}
```

**亮点**：不是简单的正面/负面计数，而是考虑了程度副词和否定词的影响，情感评分更准确。

---

## 七、AI 智能回复设计

### 7.1 流程设计

```
┌─────────────────────────────────────────────────────────────┐
│                    学生提交测评                               │
│  情绪/睡眠/学习/社交/满意度 5 维评分 + 留言（可选）           │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端处理                                   │
│  1. 保存测评数据                                              │
│  2. NLP 情感分析 → sentiment_score                           │
│  3. 如果有留言且配置了 LLM_API_KEY：                          │
│     → 构建 prompt（系统角色 + 学生信息 + 测评数据 + 留言）    │
│     → 调用 AI 模型生成回复                                    │
│     → 保存到 ai_reply 字段                                   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    学生查看测评记录                           │
│  显示：测评分数 + 留言 + AI 回复 + 辅导员回复                 │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 Prompt 设计

```python
system_prompt = """你是一位专业的大学心理咨询师，正在回复学生的心理测评留言。

你的回复需要：
1. 表达真诚的关心和理解，让学生感到被倾听
2. 对学生的情绪表示共情，不要简单否定或淡化
3. 给出具体、可操作的建议（2-3 条）
4. 语气温暖、专业，避免说教
5. 如果学生表达了严重的心理困扰（如自伤想法），要明确建议寻求专业帮助
6. 回复控制在 200-300 字以内

注意：
- 不要以"亲爱的同学"等过于正式的开头
- 不要重复学生的话
- 要有针对性地回应学生的具体问题"""

user_message = f"""
【学生信息】{student_name}（{student_id}）
【测评数据】情绪{mood}/5 睡眠{sleep}/5 学习{study}/5 社交{social}/5 满意度{life}/5 综合{overall}
【学生留言】{message}"""
```

### 7.3 ⭐ 设计亮点：自动触发 + 失败静默

```python
# AI 调用包裹在 try-except 中，失败不影响测评提交
ai_reply = None
try:
    # ... 调用 AI ...
    ai_reply = response
except Exception:
    pass  # AI 失败时 ai_reply 保持 None，不影响主流程
```

**亮点**：
- AI 回复是异步附加功能，即使 AI 服务不可用，测评提交仍然成功
- 学生不会因为 AI 故障而无法提交测评
- 配置了 API Key 才启用，未配置则跳过

### 7.4 ⭐ 设计亮点：心理咨询师聊天页面

```tsx
// 学生端独立聊天页面
const COUNSELOR_REPLY = "我特别懂你熬夜失眠...";

// 固定回复，模拟心理咨询师对话
setTimeout(() => {
  setMessages(prev => [...prev, { role: "counselor", text: COUNSELOR_REPLY }]);
}, 1500);  // 1.5 秒打字动画
```

**亮点**：
- 提供一个安全的倾诉空间，学生可以随时表达情绪
- 固定回复确保每次都能得到温暖的回应
- 打字动画模拟真实对话体验

---

## 八、安全与权限设计

### 8.1 认证流程

```
┌──────────┐     POST /login      ┌──────────┐
│  前端     │ ───────────────────→ │  后端     │
│          │ ←─────────────────── │          │
│          │  { token, role, ... }│          │
└──────────┘                      └──────────┘
     │
     │ localStorage 存储 token
     │
     │ GET /api/counselor/students
     │ Authorization: Bearer <token>
     ▼
┌──────────┐                      ┌──────────┐
│  前端     │ ───────────────────→ │  后端     │
│          │                      │          │
│          │                      │ decode_token()
│          │                      │ require_role()
│          │                      │ _get_dept_filter()
│          │ ←─────────────────── │          │
│          │  { items: [...] }    │          │
└──────────┘                      └──────────┘
```

### 8.2 ⭐ 设计亮点：JWT 载荷设计

```python
# 学生 Token
create_token({"sub": student.student_id, "role": "student"})

# 辅导员 Token（包含院系和管理员标志）
create_token({
    "sub": str(user.id),
    "role": "counselor",
    "department": user.department,
    "is_admin": user.is_admin,
})
```

**亮点**：将 `department` 和 `is_admin` 编码到 JWT 中，避免每次请求都查数据库获取用户信息。

### 8.3 权限矩阵

| 功能 | 学生 | 普通辅导员 | 管理员 |
|------|:----:|:--------:|:------:|
| 查看个人数据 | ✅ | ❌ | ❌ |
| 心理咨询聊天 | ✅ | ❌ | ❌ |
| 查看本院系学生 | ❌ | ✅ | ✅ |
| 查看全院学生 | ❌ | ❌ | ✅ |
| 触发评估 | ❌ | ✅ | ✅ |
| 查看预警 | ❌ | ✅（本院系） | ✅（全部） |
| 统计分析 | ❌ | ✅（本院系） | ✅（全部） |
| 注册辅导员 | ❌ | ❌ | ✅ |
| 定期上报 | ❌ | ✅ | ✅ |
| 回复学生留言 | ❌ | ✅ | ✅ |

---

## 九、性能优化设计

### 9.1 查询优化策略

| 场景 | 优化手段 | 效果 |
|------|----------|------|
| 获取每个学生最新记录 | CTE + ROW_NUMBER + 覆盖索引 | 避免关联子查询，60%+ 提升 |
| 周趋势统计 | 预聚合表 | 秒级 → 毫秒级 |
| 聚类分析 | student_latest_stats 单表 | 避免扫 120 万行 |
| 预警列表 | 复合索引 (risk_level, is_read, created_at) | 覆盖 WHERE + ORDER BY |
| 批量评估 | MiniBatchKMeans + 流式处理 | 10 万学生 1-2 秒完成聚类 |

### 9.2 ⭐ 设计亮点：慢查询日志

```python
@event.listens_for(engine, "after_cursor_execute")
def log_slow_query(conn, cursor, statement, parameters, context, executemany):
    start = conn.info.get("query_start_time")
    if start:
        duration_ms = (time.time() - start) * 1000
        if duration_ms > 100:
            db_logger.warning(f"Slow query ({duration_ms:.1f}ms): {statement[:200]}")
```

**亮点**：自动记录超过 100ms 的慢查询，帮助定位性能瓶颈。开发阶段即可发现潜在问题。

### 9.3 ⭐ 设计亮点：SQL 优化演示端点

```python
@router.get("/statistics/complex-query")
def run_complex_query(user: dict = Depends(require_role("counselor"))):
    # 对比慢查询（关联子查询）与优化查询（CTE + ROW_NUMBER）
    t0 = time.time()
    result = db.execute(text(fast_sql), params).fetchall()
    query_time = round((time.time() - t0) * 1000, 2)

    return {
        "optimized_query_ms": query_time,
        "data": [...]
    }
```

**亮点**：提供专门的 API 端点对比慢查询和优化查询的性能差异，前端可视化展示优化效果。

### 9.4 前端性能优化

| 优化手段 | 说明 |
|----------|------|
| TTL 缓存 | 相同请求 5 分钟内不重复发送 |
| 按需缓存 | 只缓存第一页无筛选请求 |
| 分页加载 | 每页 20 条，避免一次性加载全部数据 |
| 聚类散点限制 | 最多 2000 个点，避免前端卡顿 |
| 图表懒加载 | ResponsiveContainer 自适应尺寸 |

---

## 十、部署架构设计

### 10.1 Docker Compose 编排

```yaml
services:
  mysql:
    image: mysql:8.0
    ports: ["3306:3306"]
    environment:
      MYSQL_DATABASE: mental_health
      MYSQL_ROOT_PASSWORD: root123

  backend:
    build: ./Dockerfile.backend
    ports: ["8000:8000"]
    depends_on: [mysql]
    environment:
      DATABASE_URL: mysql+pymysql://root:root123@mysql:3306/mental_health

  frontend:
    build: ./Dockerfile.frontend
    ports: ["5173:80"]
    depends_on: [backend]
```

### 10.2 ⭐ 设计亮点：跨数据库兼容

```python
# 引擎工厂：根据 DATABASE_URL 自动选择 SQLite 或 MySQL
_is_sqlite = settings.DATABASE_URL.startswith("sqlite")

if _is_sqlite:
    engine = create_engine(url, connect_args={"check_same_thread": False})
else:
    engine = create_engine(url, pool_size=10, max_overflow=20)

# 所有 SQL 使用辅助函数
def week_label(column):
    if _is_sqlite:
        return func.strftime("%Y-W%W", column)
    else:
        return func.date_format(column, "%x-W%v")
```

**亮点**：
- 本地开发用 SQLite（零配置），生产部署用 MySQL（高性能）
- 所有 SQL 通过辅助函数兼容两种数据库，无需修改业务代码
- 自动迁移逻辑根据数据库类型选择不同的 SQL 语法

### 10.3 环境变量配置

```env
# 数据库
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/mental_health

# JWT
JWT_SECRET=your-random-secret-key
JWT_EXPIRE_MINUTES=480

# AI 模型（可选）
LLM_API_KEY=sk-your-api-key
LLM_BASE_URL=https://api.deepseek.com/v1
```

---

## 十一、设计亮点汇总

| 编号 | 亮点 | 类别 | 说明 |
|:----:|------|------|------|
| 1 | 自动迁移 + 幂等初始化 | 架构 | 启动时自动建表、迁移、建索引，无需手动 SQL |
| 2 | 预聚合表策略 | 数据库 | 百万行聚合 → 预计算统计表，查询毫秒级 |
| 3 | 触发器自动预警 | 数据库 | 高风险评估自动插入通知，数据库层面保证 |
| 4 | 覆盖索引设计 | 数据库 | MAX(id) GROUP BY 模式专用索引 |
| 5 | 视图简化查询 | 数据库 | 复杂子查询封装为视图 |
| 6 | 统一分页响应 | API | 所有列表接口返回相同格式 |
| 7 | 权限依赖注入 | API | 路由级别统一认证，一处定义全局生效 |
| 8 | 院系数据隔离 | API | admin 看全部，普通辅导员看本院系 |
| 9 | 角色自适应侧边栏 | 前端 | 管理员/辅导员/学生动态切换导航菜单 |
| 10 | JWT 载荷设计 | 安全 | department 和 is_admin 编码到 Token |
| 11 | 双 Y 轴图表 | 前端 | 不同量级指标在同一图表清晰展示 |
| 12 | 派生特征工程 | ML | 3 个交互特征提升模型表现 |
| 13 | 双层风险判定 | ML | ML 预测 + 规则引擎结合 |
| 14 | 单例懒加载模型 | ML | 模型只加载一次，节省内存 |
| 15 | 14 种干预建议 | 业务 | 多维度组合生成个性化建议 |
| 16 | 情感词典加权 | NLP | 程度副词 + 否定词影响评分 |
| 17 | AI 自动回复 | AI | 提交测评自动生成心理咨询回复 |
| 18 | 心理咨询聊天 | AI | 学生端安全倾诉空间 |
| 19 | 慢查询日志 | 性能 | 自动记录 >100ms 查询 |
| 20 | SQL 优化演示 | 性能 | 可视化对比优化效果 |
| 21 | 跨数据库兼容 | 部署 | SQLite/MySQL 无缝切换 |
| 22 | 前端 TTL 缓存 | 性能 | 智能缓存策略，避免重复请求 |
| 23 | 聚类智能命名 | 分析 | 20+ 种群体类型自动匹配 |
| 24 | 定期上报闭环 | 业务 | 学生自评 → 辅导员监测 → 汇报 |

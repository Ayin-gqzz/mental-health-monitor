# 大学生心理健康状况动态监测与预警系统

## 项目汇报

---

## 一、项目背景与目标

### 1.1 研究背景

- 高校学生心理健康问题日益突出，抑郁、焦虑等心理疾病发病率逐年上升
- 传统心理健康管理依赖人工筛查和问卷调查，存在**滞后性**和**主观性**
- 缺乏对学生日常行为数据的持续监测和量化分析手段

### 1.2 项目目标

- 构建一个**数据驱动**的心理健康动态监测平台
- 通过采集学生日常生活行为数据（睡眠、学习、运动、社交媒体、压力），利用**机器学习模型**进行抑郁风险预测
- 为辅导员提供**可视化数据分析**、**实时预警**和**干预建议**
- 实现心理健康问题的**早发现、早预警、早干预**

---

## 二、系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────┐
│           前端（React 19 + TypeScript + Vite）   │
│  端口 5173 · 代理 /api → :8000                   │
│  Zustand 状态管理 · Recharts 图表可视化          │
└───────────────────────┬─────────────────────────┘
                        │ JWT Bearer Token 认证
┌───────────────────────▼─────────────────────────┐
│              后端（FastAPI + SQLAlchemy）         │
│  端口 8000 · 3 个路由模块 · 24+ API 端点         │
│  JWT 认证 · 角色权限控制 · 慢查询日志            │
├─────────────────────────────────────────────────┤
│  服务层：                                        │
│  · MLService（懒加载 ML 模型，单例模式）          │
│  · SuggestionService（14 种干预建议模板）         │
│  · NLPService（情感分析 + 心理主题识别）          │
├─────────────────────────────────────────────────┤
│  数据层：                                        │
│  · 6 张核心表 + 2 个视图 + 1 个触发器            │
│  · 3 张预聚合表（周维度统计）                     │
│  · 跨数据库兼容（SQLite / MySQL）                │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────┐
│           ML 流水线（scikit-learn）               │
│  Random Forest + SMOTE + GridSearchCV             │
│  16 特征 → predict_proba → 风险分级              │
│  模型文件：model.pkl, scaler.pkl 等              │
└─────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术选型 |
|------|----------|
| **前端** | React 19 + TypeScript + Vite + Zustand + Recharts + Lucide React |
| **后端** | FastAPI + SQLAlchemy + Pydantic v2 + python-jose (JWT) |
| **数据库** | SQLite（开发）/ MySQL（生产 Docker） |
| **机器学习** | scikit-learn + imbalanced-learn (SMOTE) + pandas + numpy |
| **NLP** | jieba 分词 + 情感词典 + TF-IDF |
| **部署** | Docker Compose（MySQL + 后端 + 前端） |

---

## 三、核心功能

### 3.1 学生端功能

| 功能模块 | 详细说明 |
|----------|----------|
| **个人首页** | 展示个人概况、12 周行为趋势图、最新风险评估、个性化干预建议 |
| **行为记录** | 查看历史行为数据（睡眠、学习、运动、社交媒体、压力），支持分页和趋势图 |
| **评估记录** | 查看 ML 评估结果历史，包含风险等级、抑郁概率、干预建议文本 |
| **心理测评** | 每周自评问卷，包含情绪状态、睡眠质量、学习状态、社交状态、生活满意度 5 个维度 |
| **测评记录** | 查看历史测评结果及辅导员回复 |

### 3.2 辅导员端功能

| 功能模块 | 详细说明 |
|----------|----------|
| **工作台** | 统计概览（学生总数、高风险人数、平均压力、抑郁率）、风险分布饼图、各院系抑郁率柱状图（本院系高亮）、各学院辅导员汇报展示 |
| **学生管理** | 学生列表（支持学号/姓名搜索、院系/性别/风险等级筛选）、学生详情查看、触发单人/批量 ML 评估、更新辅导员备注 |
| **预警信息** | 高风险预警列表（支持性别/已读状态筛选）、标记已读/全部已读、点击跳转学生详情 |
| **统计分析** | 各院系抑郁率对比、12 周趋势图（平均压力 + 抑郁人数）、压力水平分布、院系总览表格、SQL 优化演示 |
| **统计检验** | T 检验（高/低风险组差异）、卡方检验（性别与抑郁关联）、Pearson/Spearman 相关性分析 |
| **群体画像** | K-Means 聚类（K=4）+ PCA 降维可视化、20+ 种智能群体命名、散点图 + 雷达图 + 特征分布 |
| **每周测评** | 查看学生自评列表（按情感分升序排列，最消极优先）、NLP 情感分析（情感分、关键词、心理主题）、回复学生留言 |
| **定期上报** | 辅导员每周汇报本院系学生整体心理状态、异常情况、重点关注学生 |

### 3.3 管理员端功能

| 功能模块 | 详细说明 |
|----------|----------|
| **数据总览** | 全院数据统计（不按院系过滤） |
| **预警总览** | 查看所有院系的高风险预警 |
| **注册辅导员** | 创建新的辅导员账号，分配院系 |
| **全部辅导员功能** | 统计分析、统计检验、群体画像等 |

---

## 四、数据库设计

### 4.1 核心表结构（6 张表）

| 表名 | 说明 | 关键字段 | 预估行数 |
|------|------|----------|----------|
| `student_info` | 学生基本信息 | student_id (PK), name, age, gender, department, cgpa | 100,000 |
| `behavior_log` | 行为记录 | student_id (FK), record_date, sleep_duration, study_hours, social_media_hours, physical_activity, stress_level | ~1,200,000 |
| `mental_assessment` | ML 评估结果 | student_id (FK), assessment_date, depression_predicted, depression_probability, risk_level, intervention_text | ~100,000 |
| `counselor_user` | 辅导员账号 | username, password_hash, display_name, department, is_admin | - |
| `risk_notification` | 高风险预警 | student_id (FK), risk_level, message, is_read | - |
| `weekly_assessment` | 学生每周自评 | student_id (FK), mood_score, sleep_quality, study_state, social_state, life_satisfaction, overall_score, message, counselor_reply, sentiment_score | - |
| `counselor_report` | 辅导员汇报 | counselor_id (FK), department, report_week, overall_status, abnormal_cases, key_students | - |

### 4.2 视图

| 视图名 | 说明 |
|--------|------|
| `v_latest_assessment` | 每个学生的最新评估记录（用于快速查询当前风险状态） |
| `v_student_behavior_summary` | 每个学生的行为数据汇总（平均压力、睡眠、社交等） |

### 4.3 触发器

```sql
-- 高风险评估自动触发预警通知
CREATE TRIGGER trg_high_risk_alert
AFTER INSERT ON mental_assessment
WHEN NEW.risk_level = 'high'
BEGIN
    INSERT INTO risk_notification (student_id, risk_level, message, is_read, created_at)
    VALUES (NEW.student_id, 'high',
            'Student ' || NEW.student_id || ' assessed as HIGH risk on ' || NEW.assessment_date,
            0, NEW.created_at);
END;
```

### 4.4 预聚合表

| 表名 | 说明 | 用途 |
|------|------|------|
| `weekly_behavior_stats` | 每周每院系的行为数据统计（平均压力、睡眠等） | 周趋势图（毫秒级查询） |
| `weekly_depression_stats` | 每周每院系的抑郁人数统计 | 周趋势图抑郁人数 |
| `student_latest_stats` | 每个学生的最新行为 + 评估数据 | 聚类分析、统计检验（避免百万行扫描） |

---

## 五、SQL 查询优化

### 5.1 优化策略总览

| 优化手段 | 应用场景 | 性能提升 |
|----------|----------|----------|
| **覆盖索引** | MAX(id) GROUP BY student_id 模式 | 避免全表扫描 |
| **CTE + ROW_NUMBER** | 获取每个学生最新记录 | 替代关联子查询 |
| **预聚合表** | 周维度统计数据 | 从秒级降到毫秒级 |
| **窗口函数** | 批量评估、排名查询 | 减少数据库往返 |
| **索引设计** | 高频查询路径 | 覆盖索引减少回表 |

### 5.2 具体优化案例

#### 案例 1：获取每个学生的最新行为记录

**优化前（关联子查询）：**
```sql
-- 慢查询：扫描 behavior_log 两次，无索引支持
SELECT bl.*
FROM behavior_log bl
WHERE bl.record_date = (
    SELECT MAX(b2.record_date)
    FROM behavior_log b2
    WHERE b2.student_id = bl.student_id
)
```

**优化后（CTE + ROW_NUMBER + 覆盖索引）：**
```sql
-- 快查询：单次扫描，窗口函数 + 覆盖索引
WITH latest AS (
    SELECT *, ROW_NUMBER() OVER (
        PARTITION BY student_id ORDER BY record_date DESC
    ) AS rn
    FROM behavior_log
)
SELECT * FROM latest WHERE rn = 1
```

**对应的覆盖索引：**
```sql
CREATE INDEX idx_behavior_student_id_date_desc
ON behavior_log(student_id, id DESC)
```

#### 案例 2：统计各院系风险分布

**优化前：**
```sql
-- 慢查询：每个院系执行一次子查询
SELECT department,
       (SELECT COUNT(*) FROM mental_assessment ma
        JOIN student_info si2 ON ma.student_id = si2.student_id
        WHERE si2.department = si.department AND ma.risk_level = 'high') AS high_risk
FROM student_info si
GROUP BY department
```

**优化后：**
```sql
-- 快查询：单次 JOIN + GROUP BY
SELECT si.department, ma.risk_level, COUNT(*) AS cnt
FROM student_info si
INNER JOIN mental_assessment ma ON si.student_id = ma.student_id
INNER JOIN (
    SELECT student_id, MAX(id) AS max_id
    FROM mental_assessment GROUP BY student_id
) latest ON ma.id = latest.max_id
GROUP BY si.department, ma.risk_level
```

#### 案例 3：周趋势数据查询

**优化前：**
```sql
-- 慢查询：每次请求扫描百万行原始数据
SELECT DATE_FORMAT(record_date, '%x-W%v') AS week,
       AVG(stress_level) AS avg_stress
FROM behavior_log
GROUP BY week
```

**优化后：**
```sql
-- 快查询：直接读预聚合表（~60 行，毫秒级）
SELECT year_week, avg_stress
FROM weekly_behavior_stats
WHERE department = '__ALL__'
ORDER BY year_week
```

**预聚合表刷新策略：**
```sql
-- 行为数据变化后运行一次，之后查询直接读预聚合表
INSERT INTO weekly_behavior_stats
SELECT bl.year_week, si.department,
       COUNT(DISTINCT bl.student_id),
       ROUND(AVG(bl.stress_level), 2),
       ROUND(AVG(bl.sleep_duration), 2),
       ...
FROM behavior_log bl
JOIN student_info si ON bl.student_id = si.student_id
GROUP BY bl.year_week, si.department
```

### 5.3 索引设计

```sql
-- 核心查询索引
CREATE INDEX idx_assess_student_date_desc ON mental_assessment(student_id, assessment_date DESC);
CREATE INDEX idx_behavior_student_date_desc ON behavior_log(student_id, record_date DESC);

-- 覆盖索引：MAX(id) GROUP BY student_id 模式
CREATE INDEX idx_behavior_id_student ON behavior_log(id, student_id);
CREATE INDEX idx_assess_id_student ON mental_assessment(id, student_id);

-- 预警查询索引
CREATE INDEX idx_notification_risk_read ON risk_notification(risk_level, is_read, created_at DESC);

-- 周趋势覆盖索引
CREATE INDEX idx_trends_cover ON behavior_log(record_date, year_week, stress_level);
CREATE INDEX idx_assess_trends_cover ON mental_assessment(assessment_date, year_week, depression_predicted);
```

### 5.4 跨数据库兼容

```python
# 所有 SQL 使用辅助函数兼容 SQLite 和 MySQL
def is_sqlite():
    return _is_sqlite

def week_label(column):
    """跨数据库的周标签函数"""
    if _is_sqlite:
        return func.strftime("%Y-W%W", column)
    else:
        return func.date_format(column, "%x-W%v")

def today_expr():
    """跨数据库的今日日期表达式"""
    if _is_sqlite:
        return text("(date('now'))")
    else:
        return text("(CURDATE())")
```

---

## 六、机器学习模型

### 6.1 特征工程

**原始特征（10 个）：**

| 特征 | 类型 | 说明 |
|------|------|------|
| age | 数值 | 年龄 |
| gender | 类别 | 性别（Male/Female） |
| department | 类别 | 院系（5 类） |
| cgpa | 数值 | 学分绩点 |
| sleep_duration | 数值 | 每晚睡眠时长 |
| study_hours | 数值 | 每日学习时长 |
| social_media_hours | 数值 | 每日社交媒体使用时长 |
| physical_activity | 数值 | 每周运动时长 |
| stress_level | 数值 | 压力水平（1-10） |

**派生特征（3 个）：**

| 特征 | 计算方式 | 说明 |
|------|----------|------|
| sleep_stress_ratio | sleep_duration / (stress_level + 1) | 睡眠压力比 |
| social_activity_ratio | social_media_hours / (physical_activity / 60 + 0.1) | 社交运动比 |
| study_efficiency | cgpa / (study_hours + 0.1) | 学习效率 |

### 6.2 模型训练流程

```
原始数据 (100,000 条)
    ↓
特征工程 (16 个特征)
    ↓
SMOTE 过采样 (解决类别不平衡)
    ↓
Random Forest + GridSearchCV (5 折交叉验证)
    ↓
最佳模型 → model.pkl
```

### 6.3 风险分级规则

| 风险等级 | 判定条件 |
|----------|----------|
| 🔴 **高风险** | ML 模型预测为抑郁 **或** 压力值 ≥ 8 |
| 🟡 **中风险** | 压力值 ≥ 6（且不满足高风险条件） |
| 🟢 **低风险** | 其他情况 |

### 6.4 干预建议系统（14 种模板）

| 类别 | 模板 | 触发条件 |
|------|------|----------|
| 压力 | 高压预警 / 中等压力 | stress ≥ 8 / ≥ 6 |
| 睡眠 | 严重不足 / 不足 | sleep < 4h / < 6h |
| 运动 | 缺乏 / 可提升 | activity < 60 / < 120 min |
| 社交媒体 | 过度 / 提醒 | social > 4h / > 2h |
| 学习 | 过劳 / 不足 | study ≥ 10h / ≤ 2h |
| 抑郁风险 | 专项提示（含 24h 危机热线） | 概率 ≥ 30% 或模型预测 |
| 综合 | 良好 / 轻度关注 | 无明显问题时 |

---

## 七、前端功能展示

### 7.1 辅导员工作台

- **统计卡片**：学生总数、高风险人数、平均压力、抑郁率
- **风险分布饼图**：高/中/低风险占比
- **各院系抑郁率柱状图**：本院系橙色高亮
- **各学院辅导员汇报**：卡片式展示各院系最新汇报

### 7.2 统计分析

- **周趋势折线图**：平均压力 + 抑郁人数（12 周）
- **压力分布柱状图**：各压力等级人数分布
- **院系总览表格**：学生数、平均压力、平均绩点、抑郁率、高风险数
- **SQL 优化演示**：慢查询 vs 优化查询性能对比

### 7.3 群体画像

- **K-Means 聚类**（K=4）：基于 5 个行为维度
- **智能命名**：20+ 种群体类型（高压失眠型、学业专注型、运动达人型等）
- **PCA 散点图**：2D 降维可视化
- **雷达图**：各聚类特征对比
- **特征分布图**：每个聚类的详细特征统计

### 7.4 每周测评

- **情感分排序**：最消极的学生优先展示
- **NLP 分析**：情感得分、关键词提取、心理主题识别
- **辅导员回复**：支持在线回复学生留言

---

## 八、项目创新点

### 8.1 技术创新

1. **多维特征工程**：原始 10 特征 + 3 派生特征，提升模型表现
2. **SMOTE 过采样**：解决抑郁/非抑郁类别不平衡问题
3. **预聚合表策略**：周维度统计数据预计算，查询从秒级降到毫秒级
4. **覆盖索引设计**：针对 MAX(id) GROUP BY 模式设计专用索引
5. **跨数据库兼容**：所有 SQL 使用辅助函数，支持 SQLite 和 MySQL 无缝切换

### 8.2 业务创新

1. **动态监测**：12 周行为数据持续追踪，非一次性筛查
2. **智能预警**：数据库触发器自动生成高风险预警通知
3. **群体画像**：K-Means 聚类 + 20+ 种智能命名，帮助辅导员理解学生群体特征
4. **NLP 情感分析**：对学生留言进行情感分析和心理主题识别
5. **定期上报**：辅导员每周汇报本院系整体心理状态，形成闭环管理

### 8.3 工程创新

1. **单例懒加载 ML 模型**：避免重复加载，节省内存
2. **前端缓存策略**：TTL 缓存 + 按需请求，减少网络开销
3. **角色权限控制**：管理员/辅导员/学生三级权限，数据隔离
4. **Docker 一键部署**：MySQL + 后端 + 前端容器化部署

---

## 九、数据规模

| 数据项 | 数量 |
|--------|------|
| 学生信息 | 100,000 条 |
| 行为记录 | ~1,200,000 条（12 周 × 10 万学生） |
| ML 评估 | ~100,000 条 |
| 预聚合统计 | ~180 条（12 周 × 5 院系 × 3 表） |

---

## 十、项目总结

本系统实现了从**数据采集** → **特征工程** → **ML 预测** → **风险预警** → **干预建议** → **定期汇报**的完整闭环，具备以下特点：

1. **数据驱动**：基于 10 万学生的真实行为数据，非主观判断
2. **实时预警**：高风险评估自动触发预警通知，无需人工干预
3. **智能分析**：ML 预测 + NLP 情感分析 + 聚类画像，多维度理解学生心理状态
4. **性能优化**：覆盖索引 + 预聚合表 + 跨数据库兼容，支撑 10 万级数据毫秒级查询
5. **闭环管理**：学生自评 → 辅导员监测 → 预警通知 → 干预建议 → 定期汇报

---

## 附录：项目文件结构

```
mental-health-monitor/
├── backend/                        # 后端
│   ├── app/
│   │   ├── api/                    # API 路由（24+ 端点）
│   │   ├── core/                   # 配置、数据库、安全
│   │   ├── models/                 # ORM 模型（7 个）
│   │   ├── schemas/                # Pydantic 模型
│   │   └── services/               # ML、NLP、干预建议
│   ├── ml/                         # 机器学习模型文件
│   ├── scripts/                    # 数据初始化脚本
│   └── requirements.txt
├── frontend/                       # 前端
│   └── src/
│       ├── api/                    # API 客户端
│       ├── components/             # 布局、路由守卫
│       ├── pages/                  # 页面（学生 5 个 + 辅导员 12 个）
│       └── stores/                 # Zustand 状态
├── sql/                            # DDL
├── data/                           # 数据集（10 万条）
├── docker-compose.yml              # Docker 编排
└── .env.example                    # 环境变量模板
```
